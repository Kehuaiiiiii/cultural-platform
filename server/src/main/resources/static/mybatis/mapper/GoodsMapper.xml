<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.mapper.GoodsMapper">
    <select id="getAllGoods" parameterType="Integer" resultType="Goods">
        select * from goods limit #{min},#{max}
    </select>

    <select id="getGoods"  resultType="Goods">
        select count(*) from goods where limit #{min},#{max}
    </select>

    <select id="getAllTotal" resultType="Integer">
        select count(*) from goods 
    </select>

    <select id="getTotal" parameterType="Goods" resultType="Integer">
        select count(*) from goods where id="%${id}%" and name="%${name}%" and price number weight and state=2
    </select>


</mapper>